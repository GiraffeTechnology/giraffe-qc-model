# Xavier Administrator runner dependencies. The real MNN runtime is loaded
# through the native bridge; no model-provider-specific Python package is used.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
cryptography>=42.0
