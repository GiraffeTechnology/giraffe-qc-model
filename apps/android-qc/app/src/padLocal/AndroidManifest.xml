<?xml version="1.0" encoding="utf-8"?>
<!--
  padLocal product flavor manifest.
  Explicitly removes INTERNET permission even if a library tries to add it.
  The Pad app is strictly offline: no Qwen API, no DashScope, no cloud inference.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"
        tools:node="remove" />
</manifest>
