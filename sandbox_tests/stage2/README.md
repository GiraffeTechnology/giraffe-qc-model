# Stage 2 scaffold — blocked

Do not execute this stage until Stage 1 is merged and explicitly accepted.
Q1 must select container/virtualization (and whether QEMU aarch64 fidelity is
required) or filesystem-level external-drive simulation. The selected method,
external-drive specification, mount/RW evidence, dependency differences, and
simulation limitations belong in this directory on the Stage 2 branch.

No real-hardware performance or power conclusion may be derived from this stage.
